# =============================================================================
# GO BASE PROJECT - ENVIRONMENT CONFIGURATION
# =============================================================================
# Simplified production-ready configuration
# Only change FRONTEND_URL and BACKEND_URL when deploying to new domain
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
PORT=8080

# -----------------------------------------------------------------------------
# BASE URLs - CHANGE THESE WHEN DEPLOYING TO NEW DOMAIN
# -----------------------------------------------------------------------------
# Frontend application URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Backend API URL (for OAuth callbacks, Swagger docs)
BACKEND_URL=http://localhost:8080

# Additional allowed origins for CORS (comma-separated, optional)
# Use this to allow requests from local development or other trusted sources
# Example: ADDITIONAL_ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.100:5173
ADDITIONAL_ALLOWED_ORIGINS=

# Note: CORS will automatically allow FRONTEND_URL, BACKEND_URL, and any ADDITIONAL_ALLOWED_ORIGINS

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
DATABASE_URL=postgres://username:password@localhost:5432/go_base_db?sslmode=disable

# Database Connection Pool Settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME=5m
DB_CONN_MAX_IDLE_TIME=1m

# -----------------------------------------------------------------------------
# REDIS CONFIGURATION
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# JWT CONFIGURATION
# -----------------------------------------------------------------------------
# REQUIRED: Generate a strong secret key
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production

# -----------------------------------------------------------------------------
# GOOGLE OAUTH CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Leave empty to disable Google OAuth login
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# ADMIN USER CONFIGURATION
# -----------------------------------------------------------------------------
# Default password for the super admin user
ADMIN_DEFAULT_PASSWORD=change-this-secure-password

# -----------------------------------------------------------------------------
# RATE LIMITING CONFIGURATION
# -----------------------------------------------------------------------------
RATE_LIMIT_RPS=10
RATE_LIMIT_BURST=20
RATE_LIMIT_STORAGE=memory  # Options: "memory" or "redis"

# -----------------------------------------------------------------------------
# SECURITY CONFIGURATION
# -----------------------------------------------------------------------------
# Security headers (recommended: true for production)
ENABLE_SECURITY_HEADERS=true

# Detailed tracing for debugging (recommended: false for production)
ENABLE_DETAILED_TRACING=false

# Cookie security settings
COOKIE_SECURE=true        # Set to true when using HTTPS
COOKIE_SAME_SITE=lax      # Options: "lax", "strict", "none"

# -----------------------------------------------------------------------------
# LOGGER CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Set to "true" for human-readable console logs (useful for debugging)
# Default is JSON format (production-ready)
LOGGER_CONSOLE=false

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# When deploying to a new domain:
# 1. Update FRONTEND_URL to your frontend domain (e.g., https://app.yourdomain.com)
# 2. Update BACKEND_URL to your backend domain (e.g., https://api.yourdomain.com)
# 3. Set COOKIE_SECURE=true when using HTTPS
# 4. Generate a strong JWT_SECRET
# 5. Update DATABASE_URL with your production database credentials
# 6. Configure Google OAuth if needed
# 
# CORS will automatically be configured based on FRONTEND_URL and BACKEND_URL
# =============================================================================
