# ===============================================
# Environment Variables untuk Beresin-Backend
# ===============================================
#
# Salin file ini ke .env dan isi nilainya sesuai dengan lingkungan Anda.
# JANGAN commit file .env yang berisi kredensial sensitif ke Git.

# Mode aplikasi (development, staging, production)
ENV=--

# Port untuk server API
PORT=--

FRONTEND_URL=--

# URL koneksi ke database PostgreSQL
DATABASE_URL=host=-- user=-- password=-- dbname=-- port=-- sslmode=--

# DATABASE CONNECTION POOL SETTINGS
# Jumlah maksimum koneksi yang boleh dibuka ke database.
DB_MAX_OPEN_CONNS=25
# Jumlah maksimum koneksi yang boleh menganggur di pool.
DB_MAX_IDLE_CONNS=10
# Waktu maksimum sebuah koneksi dapat digunakan kembali (mencegah koneksi basi).
DB_CONN_MAX_LIFETIME=5m
# Waktu maksimum sebuah koneksi boleh menganggur sebelum ditutup.
DB_CONN_MAX_IDLE_TIME=1m

# URL koneksi ke Redis
REDIS_URL=--

# Kunci rahasia untuk JSON Web Token (JWT).
# HARUS diisi dengan string acak yang kuat dan panjang.
# Contoh untuk generate di terminal: openssl rand -base64 32
JWT_SECRET=--

# Password default untuk pengguna admin pertama kali
ADMIN_DEFAULT_PASSWORD=--

# .env
GOOGLE_CLIENT_ID=--
GOOGLE_CLIENT_SECRET=--
